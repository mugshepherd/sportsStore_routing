import { Component } from '@angular/core';

@Component({
  selector: 'app',
  // TODO(1.4) - define routing target in app.component.ts - add new target
  template: '<router-outlet></router-outlet>'
  // TODO(1.4) - define routing target in app.component.ts - delete old targe
// template: '<store></store>'
})
export class AppComponent {
}
